#pragma once

#include "entity.h"
#include "type_id.h"

namespace ecs
{
	class Engine final
	{
	public:

		Engine()
		{ }

		void update(const float deltaTime)
		{
			// m_systemManager.update(deltaTime);
		}

	private:

	};
}